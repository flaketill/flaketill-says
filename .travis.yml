language: bash

before_script:
    - URL="https://raw.githubusercontent.com/flaketill/flaketill-says/testing/dist/aur/flaketill-says"
    - printf "Downloading git-completion..."
    - bash <(curl -s $URL)

script:
    - bash flaketill-says