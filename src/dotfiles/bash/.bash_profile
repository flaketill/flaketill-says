#
# ~/.bash_profile
# For exmaple with GNU stow
# mkdir ~/.dotfiles
# cd ~/.dotfiles
# tree -a .
# tree -a --charset ascii
# stow -t ~/.config .config/
# stow -t ~ bash/
# stow -t ~ git/
# stow -t ~ ssh/

[[ -f ~/.bashrc ]] && . ~/.bashrc